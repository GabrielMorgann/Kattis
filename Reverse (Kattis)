import java.util.*;
class Main {
  public static void main(String[] args) {
    Scanner stdin = new Scanner(System.in);
    int num = stdin.nextInt();
    ArrayList<Integer> Inputs = new ArrayList<Integer>();
    for (int i = 0; i < num; i++) {
        int x = stdin.nextInt();
        Inputs.add(x);
    }
    for (int j = num - 1; j >= 0; j--) {
      System.out.println(Inputs.get(j));
    }
    stdin.nextLine();
    }
  }


// Little Jóna needs a program. The program should read integers and print them in reverse order. Jóna asks for your help.
// Input:
// The first line of input contains the integer . Then comes a list of  integers, each on its own line.
// Output:
// The list should be printed in reverse order of input.


// Sample Input 1
// 5
// 1
// 2
// 3
// 4
// 5
// Sample Output 1
// 5
// 4
// 3
// 2
// 1
